# @expeditecommerce packages come from GitHub Packages, not public npm.
# The auth token is read from the GITHUB_TOKEN env var (set locally in your
# shell / ~/.npmrc, and in Vercel's Environment Variables) — never commit a token.
@expeditecommerce:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}