# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vendored built package — must be committed so CI/Vercel can build
# without the out-of-repo local dependency. Re-included despite `dist` above.
!vendor/next-gen-composable/dist
!vendor/next-gen-composable/dist/**
!vendor/composable-cart/dist
!vendor/composable-cart/dist/**

# Environment files (secrets — never commit)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
